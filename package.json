{
  "name": "qsp-proto-files-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/typescript"
  ],
  "scripts": {
    "lint": "buf lint",
    "generate": "buf generate",
    "build": "npm --workspace @qsp/proto-files run build"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.50.0",
    "@bufbuild/protoc-gen-es": "^2.2.3",
    "typescript": "^5.7.3"
  }
}
